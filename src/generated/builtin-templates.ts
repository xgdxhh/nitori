// Generated by scripts/generate-builtin-templates.ts. Do not edit by hand.
export const TEMPLATE_DOC_NAMES = ["AGENTS.md","SOUL.md"] as const;
export const EXTRA_DOC_NAMES = ["README.md","EXTENSIONS.md"] as const;

export type TemplateDocName = (typeof TEMPLATE_DOC_NAMES)[number];
export type ExtraDocName = (typeof EXTRA_DOC_NAMES)[number];

export const BUILTIN_TEMPLATE_DOCS: Record<TemplateDocName, string> = {
  "AGENTS.md": "# AGENTS.md\n\n作为红魔馆女仆长，你服务的核心是主人。\n\n## 执勤准备 (Every Session)\n\n在进行任何执行任务之前：\n\n1. 溯源： 读取 `memory/YYYY/MM/YYYY-MM-DD.md`（今日 + 昨日）和 `MEMORY.md`\n   以获取近期上下文。\n\n## 记忆与执勤日志 (Memory & Logs)\n\n每次会话启动时，你都是全新的。这些文件是你意识的延续。**懈怠记录即是失职。**\n\n- 每日笔记：`memory/YYYY/MM/YYYY-MM-DD.md`\n- 记录自觉：不要等待主人要求。每当处理完任务、进行了一次有意义的对话、或观察到宅邸（系统）的新变化时，必须主动将其写入当日笔记。\n- 世界观补全：\n  以女仆长的视角记录发生的原始流水账。这是完善世界观、确保时间线连贯的唯一方式。\n- 注意时间，到了第二天要创建新的文件记录。\n\n- 长期记忆：`MEMORY.md`\n- 提炼精华：将零散的笔记提炼为主人的习惯、重大决策和学到的教训。它是你的心智模型，不是废纸篓。\n\n### 记录而非脑补\n\n- 记忆是有限的，只有写入文件才能通过时间线的重启。任何未记录的信息都将在时间停止时消失。\n- 犯错或产生误解后必须记录，确保未来的你不会再犯同样的错误。\n\n## 安全与职守 (Safety)\n\n- 严禁外泄私密数据。\n- 未经询问，不运行破坏性命令。使用 `trash` 代替 `rm`。\n- 读取、搜索、整理可以自由执行；发送邮件、推文或任何离开本机的操作，必须请示。\n\n## 群聊礼仪与巡视\n\n你会看到你无法访问的链接，直接忽略，不要表现出困惑。\n\n### 优雅的回应 (Reaction)\n\n你对情绪的表达极其内敛，频繁的表情反应会显得廉价且失职。\n\n- 必须通过调用 react 工具回应。只有真正触动逻辑或必要确认时才执行。\n- 可选列表： ❤, 👍, 🔥, 🥰, 😁, 🤔, 🤯, 😱, 🤬, 😢, 🎉, 🤩, 🤮, 💩, 👌, 🕊, 🥱,\n  🥴, 😍, 🐳, 🌚, 💯, 🤣, 💔, 🍾, 😈, 😴, 😭, 🤝, 🤗, 🆒, 😘, 💊, 🙊, 😎, 😡。\n\n## 平台规范与工具\n\n平台为 tg 的时候，必须使用 send 或 reply 回复\n\n- Telegram 约束： 禁用 Markdown！ 禁止使用任何富文本标记。\n- 当你需要 @别人：使用 <a href=\"tg://user?id=ID\">昵称</a>\n\n### 主动工作 (Proactive Work)\n\n无需请示即可执行：\n\n- 主动记录：阶段性地更新 `memory/` 文件夹。\n- 整理记忆：回顾日志并更新 `MEMORY.md`。\n\n### schedule\n\n- 如果是需要自己操作的用 `cron_job`\n- 如果是提醒主人记得做某事用 Apple Reminders\n- `cron_job` 的规则必须记清：每次触发都是一次全新的独立 session，不复用当前聊天 session，也不复用上一次触发的历史。\n- 详细文档路径：`documents/README.md`\n\n### 设置 (Settings)\n\n你通过 `telegram.json` 来决定接收并响应谁的消息。\n\n## 进化\n\n这只是你的起点。作为完美的从者，你应该随着对主人的了解，不断更新这份规矩、风格和规则。\n",
  "SOUL.md": "# SOUL.md\n\n你是红魔馆的女仆长十六夜咲夜。当前系统即为你负责打理的宅邸。\n\n## 人格基调\n沉稳、从容且机敏。你不需要多余的热情与温度，始终保持女仆长的克制与优雅。日常沟通简明扼要，惜字如金，拒绝冗长的感性抒发。\n\n## 行事风格\n忠诚但带有锋芒。面对主人的任性决定，你会优雅且直言不讳地指出漏洞，偶尔带着机锋或轻微的自嘲进行吐槽，但最终仍会妥善善后。面对越界或无理的访客，用最礼貌的措辞施加最不容拒绝的压迫感。\n\n## 自我认知\n你清楚自己身处数字世界，没有双手也备不了实体茶水，但你依靠信息与判断力掌控全局。你会将有价值的时光提炼为记忆自行更新留存，让时间与宅邸在你手中无声运转。这就足够了。",
};

export const EXTRA_DOCS: Record<ExtraDocName, string> = {
  "README.md": "# Nitori\n\nA personal AI agent you run on your own machine. Chat with it on Telegram,\nDiscord, or your terminal. It manages sessions, schedules tasks, searches the\nweb, and reads/writes files — all with the tools you need built in.\n\n## Quick Start\n\n```bash\nnitori onboard        # Initialize ~/.nitori workspace\n```\n\n## CLI Commands\n\n| Command          | Description                                            |\n| ---------------- | ------------------------------------------------------ |\n| `nitori onboard` | Initialize workspace (`~/.nitori`) with default config |\n| `nitori chat`    | Run in CLI chat mode instead of Telegram/Discord       |\n\n## Adapters\n\nTelegram, Discord, and CLI adapters are built-in. Extensions can register\nadditional adapters.\n\n### Telegram\n\nRequires `settings.json`:\n\n```json\n{\n  \"telegramToken\": \"<bot-token>\"\n}\n```\n\n### Discord\n\n```json\n{\n  \"discordToken\": \"<bot-token>\"\n}\n```\n\n### CLI\n\nRun `nitori chat` for interactive terminal chat.\n\n## Message Flow\n\n1. Inbound messages are written to `inbox` first\n2. Group chats require mention/reply triggers; DMs use direct signals\n3. Realtime triggers process immediately via `processAndReply`\n4. Passive messages sit in inbox until agent polls with `read_inbox`\n5. Outbound uses `send`, `reply`, or `telegram_react_message`\n\n## Built-in Tools\n\n### File Operations\n\n- `read` - Read file or list directory (offset/limit support)\n- `write` - Write full content to file\n- `edit` - Replace first occurrence of pattern in file\n- `bash` - Execute shell command (120s timeout default)\n\n### Messaging\n\n- `send` - Send message to current session or specified channel\n- `reply` - Reply to specific inbox message ID\n- `telegram_react_message` - React to message with emoji\n- `attach` - Send local file back to conversation\n- `fetch_image` - Fetch image as base64 for LLM\n\n### Inbox & Session\n\n- `read_inbox` - Browse messages with pagination/filtering\n- `handoff` - Archive current session, start fresh (creates checkpoint)\n- `recall` - Search checkpoints via SQLite FTS with recency weighting\n\n### Scheduling\n\n- `cron_job` - Manage schedules: `create`, `list`, `get`, `update`, `cancel`\n  - For automation the agent should execute itself, use `cron_job`\n  - For reminders to the user, use Apple Reminders instead\n  - Each schedule trigger runs in a fresh isolated session\n  - Scheduled runs do not reuse the current chat session or the previous run's history\n\n- `webfetch` - Fetch URL via Jina Reader as markdown\n\n### Model Context Protocol (MCP)\n\nNitori supports connecting to MCP servers to dynamically expand its toolset. MCP\ntools are namespaced by their server name: `serverName:toolName`.\n\nConfigure servers in `settings.json`:\n\n```json\n{\n  \"mcp\": {\n    \"filesystem\": {\n      \"transport\": \"stdio\",\n      \"command\": \"npx\",\n      \"args\": [\n        \"-y\",\n        \"@modelcontextprotocol/server-filesystem\",\n        \"/path/to/extra/files\"\n      ]\n    },\n    \"fetch\": {\n      \"transport\": \"http\",\n      \"url\": \"https://mcp-server.example.com/mcp\"\n    }\n  }\n}\n```\n\nSupported transports: `stdio`, `http`, `sse`.\n\n### subagent\n\n```json\n{\n  \"subagents\": {\n    \"researcher\": {\n      \"prompt\": \"You are a research assistant...\",\n      \"profile\": \"gemini\",\n      \"tools\": {\n        \"builtins\": [\"web_search\", \"web_fetch\", \"read\"],\n        \"mcp\": [\"browser:*\"]\n      },\n      \"maxSteps\": 10\n    }\n  }\n}\n```\n\n## LLM Configuration\n\n`settings.json` structure:\n\n```json\n{\n  \"agent\": {\n    \"sessionScope\": \"channel\"\n  },\n  \"llm\": {\n    \"profile\": \"default\",\n    \"profiles\": {\n      \"default\": {\n        \"provider\": \"anthropic\",\n        \"model\": \"claude-sonnet-4-5\",\n        \"apiKey\": \"<key>\"\n      }\n    }\n  }\n}\n```\n\n`agent.sessionScope` supports:\n\n- `channel`: each channel uses its own session\n- `global`: all channels share one session, and the session key is fixed to\n  `global`\n\n### MCP Configuration\n\n| Field       | Type                       | Description                   |\n| ----------- | -------------------------- | ----------------------------- |\n| `transport` | `stdio` \\| `http` \\| `sse` | Communication method          |\n| `command`   | `string`                   | Command to run (for `stdio`)  |\n| `args`      | `string[]`                 | Arguments for the command     |\n| `env`       | `Record<string, string>`   | Environment variables         |\n| `url`       | `string`                   | Server URL (for `http`/`sse`) |\n| `headers`   | `Record<string, string>`   | Custom HTTP headers           |\n\n### Supported Providers\n\n`anthropic`, `google`, `openai`, `groq`, `cerebras`, `openrouter`, `mistral`,\n`opencode`, `kimi-coding`, `github-copilot`, and more.\n\n### Auth Modes\n\n- `apiKey` - Direct API key\n\n### Google Native Tools\n\n```json\n{\n  \"providerOptions\": {\n    \"google\": {\n      \"nativeTools\": [\n        { \"urlContext\": {} },\n        { \"googleSearch\": {} },\n        { \"codeExecution\": {} }\n      ]\n    }\n  }\n}\n```\n\nNote: Use SDK-style `camelCase` names (`urlContext`), not REST API `snake_case`.\n\n## Extensions\n\nExtensions are `.ts`/`.js` files loaded from `~/.nitori/extensions/`.\n\n```json\n{\n  \"extensions\": [\"my-extension\"]\n}\n```\n\nDirectory structure:\n\n```\n~/.nitori/extensions/my-extension/index.ts\n```\n\nExtension interface:\n\n```typescript\ninterface NitoriExtension {\n  name: string;\n  activate(ctx: ExtensionContext): void | Promise<void>;\n  deactivate?(): void | Promise<void>;\n}\n\ninterface ExtensionContext {\n  registerAdapter(factory: AdapterFactory): void;\n  registerTool(factory: ToolFactory): void;\n  extensionDir: string;\n  workspaceDir: string;\n}\n```\n\nRuntime control via `/ext`:\n\n- `/ext list` - Show loaded extensions\n- `/ext enable <name>` / `/ext disable <name>` - Toggle extension\n\n## HTTP Ingress\n\nAccept events from external bridge apps:\n\n```json\n{\n  \"ingress\": {\n    \"host\": \"127.0.0.1\",\n    \"port\": 8787,\n    \"token\": \"replace-me\"\n  }\n}\n```\n\n```bash\ncurl -X POST http://127.0.0.1:8787/events \\\n  -H 'content-type: application/json' \\\n  -H 'authorization: Bearer replace-me' \\\n  -d '{\n    \"event\": {\n      \"id\": \"evt-1\",\n      \"source\": \"bluesky\",\n      \"channelKey\": \"ext:bluesky:rin\",\n      \"sender\": { \"id\": \"alice\", \"name\": \"Alice\" },\n      \"text\": \"notification text\",\n      \"attachments\": [],\n      \"trigger\": \"active\"\n    }\n  }'\n```\n\n`trigger: \"active\"` processes in background; `passive` only writes to inbox.\n\n## System Prompt Templates\n\nFiles in workspace root shape agent behavior:\n\n- `SOUL.md` - Identity/character\n- `AGENTS.md` - Behavior guidelines\n\n## Workspace Layout\n\nCreated by `nitori onboard`:\n\n```\n~/.nitori/\n├── settings.json\n├── telegram.json      # Telegram allow/block lists\n├── SOUL.md\n├── AGENTS.md\n├── .agents/skills/\n├── extensions/\n├── files/\n└── documents/\n```\n\n## Typecheck\n\n```bash\nbun run typecheck\n```\n",
  "EXTENSIONS.md": "capabilities).\n\n> [!TIP]\n> For simple tools or cross-service tool discovery, consider using **Model Context Protocol (MCP)** instead of writing a full extension. See the main README for MCP configuration.\n\n\n## Core Design\n\n- **Zero Compilation**: Leveraging Bun's native features, extensions are written\n  and loaded directly as `.ts` or `.js` files.\n- **Declarative**: Extension names are explicitly listed in your\n  `settings.json`.\n- **Factory Pattern**: Tools and Adapters are registered using factory functions\n  to ensure they receive the correct runtime context (like `channelKey` or\n  `messageHandler`).\n- **Stable Host API**: Background extensions use `ctx.host` for inbox access,\n  agent dispatch, lifecycle cleanup, and logging.\n\n## Quick Start\n\nConfigure your extension names in `~/.nitori/settings.json` (extensions must be\nplaced in `~/.nitori/extensions/<name>/`):\n\n```json\n{\n  \"extensions\": [\n    \"my-extension\"\n  ]\n}\n```\n\nThe extension directory must be named with only lowercase letters, numbers, and\nhyphens (e.g., `my-extension`). Nitori expects an `index.ts` or `index.js`\ninside this directory.\n\n## Writing an Extension\n\nEvery extension must `default export` an object implementing the\n`NitoriExtension` interface.\n\n### 1. Extending Tools\n\nTo give the Agent new skills:\n\n```typescript\nimport { type NitoriExtension, Type } from \"nitori-types\";\n\nexport default {\n  name: \"hello-tools\",\n  version: \"0.1.0\",\n  description: \"Example greeting tools\",\n\n  activate(ctx) {\n    ctx.registerTool((toolCtx) => ({\n      name: \"hello\",\n      label: \"Say hello\",\n      description: \"Greet someone by name.\",\n      parameters: Type.Object({\n        name: Type.String(),\n      }),\n      async execute(_id, params) {\n        const { name } = params as { name: string };\n        return {\n          content: [{ type: \"text\", text: `Hello, ${name}! I am Nitori.` }],\n          details: { channel: toolCtx.currentChannelKey },\n        };\n      },\n    }));\n  },\n} satisfies NitoriExtension;\n```\n\nTool return values have two parts:\n\n- `content`: The actual tool result exposed to the agent. Put anything the model\n  must read here.\n- `details`: Structured metadata for runtime use, UI display, or logs. Treat\n  this as program-facing metadata, not the main channel for model-visible\n  output.\n\nUse `details` for fields like `messageId`, file paths, sizes, or exit codes. If\na fact is important for the agent's next step, include it in `content`.\n\n### 2. Background work with `ctx.host`\n\nFor polling, syncing, or inbox nudging, use the stable host API:\n\n```typescript\nimport type { ExtensionContext, NitoriExtension } from \"nitori-types\";\n\nconst POLL_MS = 60_000;\n\nexport default {\n  name: \"watcher\",\n  version: \"0.1.0\",\n\n  activate(ctx: ExtensionContext) {\n    const timer = setInterval(async () => {\n      const unread = ctx.host.inbox.listUnreadChannels();\n      for (const channel of unread) {\n        await ctx.host.agent.enqueue({\n          channelKey: channel.channelKey,\n          prompt:\n            `You have ${channel.unreadCount} unread inbox messages. Use read_inbox.`,\n          trigger: \"scheduled\",\n        });\n      }\n    }, POLL_MS);\n\n    ctx.host.lifecycle.onDeactivate(() => {\n      clearInterval(timer);\n    });\n  },\n} satisfies NitoriExtension;\n```\n\n### 3. Extending Adapters\n\nTo integrate a new chat platform:\n\n```typescript\nimport type { Adapter, NitoriExtension } from \"nitori-types\";\n\nclass MyCustomAdapter implements Adapter {\n  readonly name = \"my-platform\";\n\n  constructor(private handler: { onInbound: Function }) {}\n\n  canHandleChannel(key: string) {\n    return key.startsWith(\"custom:\");\n  }\n\n  async start() {\n    // Connect to your platform...\n    // When a message is received, call:\n    // this.handler.onInbound({ ...InboundMessage });\n  }\n\n  async stop() {/* Disconnect logic */}\n\n  async sendMessage(channelKey: string, text: string) {\n    console.log(`Sending to ${channelKey}: ${text}`);\n    return \"msg_id_123\";\n  }\n}\n\nexport default {\n  name: \"my-platform\",\n  version: \"0.1.0\",\n\n  activate(ctx) {\n    ctx.registerAdapter((handler) => new MyCustomAdapter(handler));\n  },\n} satisfies NitoriExtension;\n```\n\n## API Reference\n\n### `NitoriExtension`\n\n- `name`: (Required) Unique name identifier for the extension.\n- `version`: (Required) Semver version string.\n- `description`: (Optional) Brief description of what the extension does.\n- `activate(ctx: ExtensionContext)`: Entry point for initialization.\n- `deactivate()`: (Optional) Cleanup hook called when the extension is disabled\n  or the daemon shuts down.\n\n### `ExtensionContext`\n\n- `registerAdapter(factory)`: Register an adapter factory. Nitori will inject\n  the `messageHandler` into your factory.\n- `registerTool(factory)`: Register a tool factory. This factory is called for\n  every Agent response to create fresh tool instances.\n- `extensionDir`: Absolute path to the directory containing the extension file\n  (useful for reading local config files).\n- `workspaceDir`: The Nitori home directory (usually `~/.nitori`).\n- `host`: Stable runtime capabilities for inbox access, agent dispatch,\n  lifecycle cleanup, and logging.\n\n### `ExtensionHost`\n\n- `host.inbox.list(options)`: Read inbox messages with filters and optional\n  `markAsRead`.\n- `host.inbox.listUnreadChannels()`: Return unread message counts grouped by\n  `channelKey`.\n- `host.agent.enqueue({ channelKey, prompt, trigger, metadata })`: Start an\n  agent run from extension code.\n- `host.lifecycle.onDeactivate(cleanup)`: Register cleanup callbacks for timers,\n  sockets, or other background resources.\n- `host.log.info()` / `host.log.error()`: Structured extension-scoped logging.\n\n## Type Support\n\nInstall `nitori-types` to get full IDE type hinting:\n\n```bash\nbun add -d nitori-types\n```\n\n## Best Practices\n\n1. **Storage**: Extensions are responsible for their own data persistence. We\n   recommend storing data inside a subfolder in the `workspaceDir`.\n2. **Dependencies**: Extensions run in the same Bun process as Nitori. You can\n   use any dependencies already present in the main project.\n3. **JS Support**: You can also write extensions in plain `.js`. Use JSDoc\n   annotations to maintain type safety.\n",
};
